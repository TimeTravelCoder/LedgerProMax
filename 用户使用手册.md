# 📖 Ledger Pro Max — 用户使用手册

> **版本**：v3.0.0 · **系统要求**：Windows 10 / 11（64位）

---

## 目录

1. [安装与启动](#1-安装与启动)
2. [首次使用：配置工作空间](#2-首次使用配置工作空间)
3. [数据看板](#3-数据看板)
4. [智能收集箱](#4-智能收集箱)
5. [分类工作空间](#5-分类工作空间)
6. [3-2-1 备份管家](#6-3-2-1-备份管家)
7. [智能查重清理](#7-智能查重清理)
8. [控制面板设置](#8-控制面板设置)
9. [自动更新](#9-自动更新)
10. [常见问题](#10-常见问题)

---

## 1. 安装与启动

### 下载安装包

前往 [GitHub Releases](https://github.com/TimeTravelCoder/LedgerProMax/releases/latest) 下载：

- **`*_x64-setup.exe`** — 推荐，NSIS 安装向导
- **`*_x64_zh-CN.msi`** — WiX MSI 安装程序

### 安装步骤

1. 双击 `setup.exe`，点击「下一步」完成安装
2. 桌面或开始菜单会出现 **👑 Ledger Pro Max** 图标
3. 双击启动，首次打开会进入配置向导

> ⚠️ 如果 Windows Defender 弹出警告，点击「更多信息」→「仍要运行」即可，这是未经微软认证的正版应用的正常提示。

---

## 2. 首次使用：配置工作空间

首次启动后，需要先配置工作空间路径：

### 第①步：设置工作空间目录

1. 点击左侧菜单 **⚙️ 设置**
2. 选择「路径管理」选项卡
3. 点击「工作空间路径」右侧的文件夹图标，选择一个空目录

> 💡 **建议**：选择容量充足的磁盘（如 `D:\LedgerWorkspace`），不要放在系统盘 C 盘。

### 第②步：初始化标准目录

1. 配置好路径后，点击「初始化工作空间」按钮
2. 系统会自动创建 12 个标准分类目录

### 第③步：配置备份路径（可选但推荐）

在「路径管理」中设置：
- **磁盘备份**：外置硬盘或另一个本地磁盘路径
- **云备份**：OneDrive / 坚果云等同步目录路径

配置完成后，点击左侧「📊 看板」查看整体状态。

---

## 3. 数据看板

看板是软件主页，展示工作区全景信息。

### 信号网格（顶部四格）

| 指标 | 含义 |
|------|------|
| 📥 待处理 | 收集箱中尚未归档的文件数 |
| 🤖 AI 覆盖率 | 已打标签文件的占比 |
| 🛡️ 备份评分 | 3-2-1 备份健康度（0-100分）|
| 📁 文件总量 | 工作区内所有文件数 |

### 标签热度图

展示使用最多的 5 个标签，**点击标签**可直接跳转工作空间并按该标签筛选。

### 近 7 天整理趋势

鼠标悬停在折线图上，查看每天具体的整理文件数量。

### 桌面健康度

- 绿色：桌面文件数 < 5，状态良好
- 黄色：5-15 个，建议清理
- 红色：> 15 个，点击「一键清理」按钮迁移到收集箱

---

## 4. 智能收集箱

收集箱是文件进入管理系统的第一站。

### 添加文件到收集箱

**方式一：拖拽**
- 从文件资源管理器拖拽文件到收集箱区域，直接松手即可

**方式二：目录监控自动弹窗**
- 在设置中配置「监控目录」（如下载文件夹）
- 有新文件进入时，右下角会弹出通知
- 点击通知，选择要导入的文件，点击「导入收集箱」

**方式三：桌面一键清理**
- 点击「清理桌面」按钮
- 勾选要移入收集箱的文件
- 点击「确认移入」

### 整理收集箱中的文件

对收集箱中的每个文件，你可以：

1. **打标签** — 点击文件右侧的标签区域，选择或输入标签
   - 支持 AI 自动推荐（点击✨图标）
2. **填备注** — 输入文件描述信息
3. **选目标目录** — 下拉选择要归档到哪个工作空间目录
4. **改名（可选）** — 使用命名模板自动生成规范文件名
5. **点击归档** — 文件移入工作空间对应目录

### 命名模板变量

| 变量 | 含义 | 示例 |
|------|------|------|
| `{date}` | 当天日期 | `20260607` |
| `{topic}` | 文件主题（手动填写）| `机器学习` |
| `{version}` | 版本号 | `v1` |
| `{status}` | 状态标签 | `草稿` |

示例：模板 `{date}_{topic}_{version}` → 输出 `20260607_机器学习_v1.pdf`

---

## 5. 分类工作空间

工作空间是已整理文件的主要查看和管理界面。

### 搜索文件

- **关键词搜索** — 在搜索框输入文件名关键词
- **拼音首字母** — 输入中文拼音首字母，如 `jqbg` 匹配 `季度报告`
- **标签筛选** — 点击标签下拉框，按标签过滤
- **类型筛选** — 按文件扩展名（PDF / Word / Excel / 图片等）筛选
- **状态筛选** — 按文件状态（进行中 / 已完成 / 归档等）筛选

### 文件操作

**单击文件** → 右侧弹出预览面板

**右键文件** → 弹出操作菜单：
- 「打开」— 用系统默认程序打开
- 「重命名」— 支持修改文件名，会自动更新数据库记录
- 「编辑标签」— 修改文件标签
- 「删除」— 删除文件（不可恢复，请谨慎）

### 视图切换

点击右上角的 ☰ / ⊞ 图标切换列表视图和网格视图。

### ZIP 归档打包

1. 勾选多个文件（列表视图下勾选复选框）
2. 点击「ZIP 归档」按钮
3. 确认归档后，文件被压缩到 `10_归档区`，源文件自动清理

### 新建项目目录

1. 选择目标工作空间目录
2. 点击「初始化项目」按钮
3. 自动创建标准项目结构：`docs / src / data / assets / output / test`

---

## 6. 3-2-1 备份管家

### 什么是 3-2-1 备份？

| 数字 | 含义 |
|------|------|
| **3** | 保留 3 份数据（原件 + 2份备份）|
| **2** | 存在 2 种不同介质（内置盘 + 外置盘/云盘）|
| **1** | 至少 1 份存放在异地（云盘）|

### 执行备份

1. 点击左侧「🛡️ 备份」
2. 查看备份健康评分和上次备份时间
3. 点击「立即备份」→「磁盘备份」或「云备份」
4. 备份进度实时显示，完成后显示本次备份文件数和总大小

> 💡 **建议**：每周至少执行一次全量备份，每天工作结束后执行一次增量备份。

### 查看备份历史

备份历史记录每次操作的：
- 备份时间
- 复制文件数 / 跳过文件数（未变化）
- 数据总量
- 执行状态（成功 / 失败）

---

## 7. 智能查重清理

### 查重步骤

1. 点击左侧「🔍 查重」
2. 选择查重模式：
   - **文件名查重** — 快速，找出同名文件
   - **文件大小查重** — 快速，找出体积相同的文件
   - **SHA-256 精确查重** — 较慢，但零误报，内容完全一致才认定为重复
3. 点击「开始扫描」
4. 扫描结果按组展示，每组内的文件为重复项

### 删除重复文件

- 勾选要删除的文件（建议保留最新版本或路径最清晰的那份）
- 点击「删除选中」
- 二次确认后永久删除

---

## 8. 控制面板设置

### 路径管理

| 路径 | 作用 |
|------|------|
| 工作空间 | 所有文件的主目录 |
| 监控目录 | 自动检测新文件的目录（如下载文件夹）|
| 磁盘备份 | 本地第二块磁盘或外置硬盘 |
| 云备份 | OneDrive / 坚果云等同步目录 |

### 标签管理

软件支持三级标签体系：

| 级别 | 示例 |
|------|------|
| 主标签 | `学习` `项目` `个人` |
| 副标签 | `机器学习` `毕业论文` `财务` |
| 状态标签 | `进行中` `已完成` `待审核` `归档` |

点击「添加标签」可新建标签，拖拽可调整顺序，点击删除图标可移除。

### 自动分类规则

规则格式：**关键词 + 扩展名 → 目标目录**

示例规则：
- 关键词含「论文」且扩展名为 `.pdf` → 自动推荐归档到 `05_学术论文`
- 扩展名为 `.ipynb` → 自动推荐归档到 `04_代码仓库`

点击「测试规则」，输入文件名，实时预览规则会推荐哪个目录。

### 主题切换

点击右上角的 🌙 / ☀️ 图标切换：
- **赛博暗黑**（默认）— 深色界面，护眼适合夜间使用
- **极简明亮** — 浅色界面，适合白天光线充足环境

---

## 9. 自动更新

软件启动后约 **3 秒**内，会自动向服务器查询是否有新版本。

- **有新版本** → 弹出更新提示，显示版本号和更新内容，点击「立即更新」自动下载安装
- **无新版本** → 静默，不打扰正常使用

更新过程：
1. 后台下载安装包（进度条显示）
2. 下载完成后验证签名（确保安装包来自官方）
3. 提示「重启软件完成更新」
4. 关闭并重启软件，自动完成升级

> 💡 手动检查更新：点击左侧「ℹ️ 关于」→「检查更新」按钮

---

## 10. 常见问题

### Q：软件安装后无法打开？
- 确认系统为 Windows 10/11 64位
- 检查是否被杀毒软件拦截，在杀毒软件中添加信任
- 尝试右键「以管理员身份运行」

### Q：首次扫描工作区很慢？
正常现象。首次扫描需要建立数据库索引，文件越多越慢。后续增量扫描会快很多。

### Q：备份目录设置后显示「路径不可达」？
- 确认外置硬盘已正确连接
- 检查路径是否正确（磁盘盘符是否变化）
- 确认对该目录有写入权限

### Q：文件删除后能恢复吗？
**不能直接恢复**。软件的删除是永久物理删除，不经过回收站。建议删除前先确认。

如果有备份，可以从备份目录手动找回。

### Q：查重扫描中途停止？
点击重新开始即可。SHA-256 模式耗时较长（文件多时可能需要几分钟），请耐心等待。

### Q：自动更新失败怎么办？
手动下载最新版本安装包：
[https://github.com/TimeTravelCoder/LedgerProMax/releases/latest](https://github.com/TimeTravelCoder/LedgerProMax/releases/latest)

直接安装即可升级，无需卸载旧版本。

---

*Ledger Pro Max v3.0.0 · 最后更新：2026-06-07*
