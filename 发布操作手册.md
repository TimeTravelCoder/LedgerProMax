# 🚀 Ledger Pro Max — 发布操作手册

> **一句话原则**：随时 `push` 代码不会触发编译，只有推送版本标签才启动自动发布。

---

## 📋 完整发布流程图

```
日常开发
  │
  ▼
git push origin ProMax    ← 随时推，绝不触发编译 ✅
  │
  │  （继续开发...随时 push...）
  │
  ▼
准备好发新版本了
  │
  ├─ ① 修改四处版本号
  ├─ ② commit + push 代码
  └─ ③ 打版本标签 + push 标签  ← 唯一触发编译的操作！
           │
           ▼
    GitHub Actions 自动运行（约 10 分钟）
           │
           ├── 编译 MSI + EXE 安装包
           ├── 用私钥签名
           ├── 生成 updater.json
           └── 创建 GitHub Release，上传全部文件
                      │
                      ▼
             已安装旧版本的用户
             启动软件 → 3 秒后自动弹出更新提示 ✅
```

---

## 📝 每次发版操作步骤

### 第①步：修改四处版本号

每次发版必须同步修改以下四个文件：

| 文件 | 找到这行 | 改为（示例） |
|------|---------|------------|
| `package.json` | `"version": "3.0.0"` | `"version": "3.1.0"` |
| `src-tauri/tauri.conf.json` | `"version": "3.0.0"` | `"version": "3.1.0"` |
| `src-tauri/Cargo.toml` | `version = "3.0.0"` | `version = "3.1.0"` |
| `src/components/pages/AboutPage.tsx` | `v3.0.0`（共两处） | `v3.1.0` |

---

### 第②步：提交代码并推送（不触发编译）

```powershell
git add -A
git commit -m "release: v3.1.0 - 这里写本次更新内容"
git push origin ProMax
```

> ✅ 此步骤**不会触发任何编译**，可以放心推送。

---

### 第③步：推送版本标签（触发编译！）

```powershell
git tag v3.1.0
git push origin v3.1.0
```

> 🚀 **推送标签后，GitHub Actions 立即开始自动编译发布！**

---

## ⚡ 一键发布脚本

修改第一行的版本号，一次性复制执行：

```powershell
$ver = "3.1.0"   # ← 只改这里！

git add -A
git commit -m "release: v$ver"
git push origin ProMax
git tag "v$ver"
git push origin "v$ver"

Write-Host ""
Write-Host "✅ 已触发 v$ver 自动编译发布！" -ForegroundColor Green
Write-Host "📊 查看进度：https://github.com/TimeTravelCoder/LedgerProMax/actions" -ForegroundColor Cyan
Write-Host "📦 发布列表：https://github.com/TimeTravelCoder/LedgerProMax/releases" -ForegroundColor Cyan
```

---

## 🔍 查看编译进度

**方式一：命令行实时查看**
```powershell
gh run list --repo TimeTravelCoder/LedgerProMax --limit 5
```

**方式二：浏览器**

👉 https://github.com/TimeTravelCoder/LedgerProMax/actions

---

## 🗑️ 标签打错了？

```powershell
# 删除本地标签
git tag -d v3.1.0

# 删除远端标签
git push origin --delete v3.1.0

# 重新打正确的标签
git tag v3.1.0
git push origin v3.1.0
```

---

## 📁 编译成功后 Release 包含的文件

| 文件 | 说明 |
|------|------|
| `*_x64-setup.exe` | **推荐** NSIS 安装包 |
| `*_x64-setup.exe.sig` | 安装包签名（自动更新用） |
| `*_x64_zh-CN.msi` | WiX MSI 安装包 |
| `*_x64_zh-CN.msi.sig` | MSI 签名 |
| `updater.json` | 自动更新描述文件（app 启动时拉取） |

---

## ✅ 一次性配置（已完成，无需重复操作）

| 项目 | 状态 |
|------|------|
| GitHub Actions 工作流 `.github/workflows/build.yml` | ✅ 已配置（仅 Tag 触发） |
| 签名私钥 `TAURI_SIGNING_PRIVATE_KEY` | ✅ 已上传 GitHub Secrets |
| 签名密码 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` | ✅ 已上传 GitHub Secrets |
| `tauri.conf.json` 更新器端点 | ✅ 已配置 |
| 私钥本地备份 | ✅ `C:\Users\Apollo\OneDrive\C.档案\密钥\ledgerpromax_v3.key` |

---

*最后更新：2026-06-07 · Ledger Pro Max v3.0.0*
