# Ledger Pro Max — 完整开发流程教程

> **适用范围**：本文档描述从零开始创建项目、日常功能开发、版本迭代到正式发布的完整工作流。  
> **技术栈**：Tauri v2 · Rust · React 19 · TypeScript · Vite · SQLite  
> **仓库地址**：https://github.com/TimeTravelCoder/LedgerProMax

---

## 目录

1. [项目架构总览](#1-项目架构总览)
2. [开发环境搭建（仅首次）](#2-开发环境搭建仅首次)
3. [立项流程](#3-立项流程)
4. [日常开发流程](#4-日常开发流程)
5. [版本迭代规范](#5-版本迭代规范)
6. [发布上线流程](#6-发布上线流程)
7. [自动更新机制说明](#7-自动更新机制说明)
8. [分支管理策略](#8-分支管理策略)
9. [Commit 规范](#9-commit-规范)
10. [常见问题速查](#10-常见问题速查)

---

## 1. 项目架构总览

```
LedgerProMax/
├── src/                        # 前端 React 源码
│   ├── App.tsx                 # 主入口 · 全局状态管理 · 事件路由
│   ├── index.css               # 全局样式 · CSS 变量 · 主题系统
│   ├── components/
│   │   ├── pages/              # 各功能页面组件
│   │   │   ├── DashboardPage.tsx   # 数据看板
│   │   │   ├── InboxPage.tsx       # 智能收集箱
│   │   │   ├── WorkspacePage.tsx   # 分类工作空间
│   │   │   ├── BackupPage.tsx      # 3-2-1 备份管理
│   │   │   ├── SettingsPage.tsx    # 系统设置
│   │   │   └── AboutPage.tsx       # 关于 & 更新
│   │   ├── DuplicateFinder.tsx # 智能查重
│   │   ├── PreviewPanel.tsx    # 文件预览面板
│   │   └── ZipArchiveModal.tsx # ZIP 归档弹窗
│   ├── types/index.ts          # 全局 TypeScript 类型定义
│   └── utils/fileUtils.tsx     # 工具函数 · 文件图标 · 格式化
│
├── src-tauri/                  # Rust 后端源码
│   ├── src/
│   │   ├── lib.rs              # Tauri 命令注册 · 应用入口
│   │   ├── db.rs               # SQLite 数据库操作
│   │   ├── file_manager.rs     # 文件管理 · 扫描 · 归档
│   │   ├── backup.rs           # 备份管理器
│   │   ├── watcher.rs          # 目录监听器（notify）
│   │   ├── config_manager.rs   # 配置持久化
│   │   └── semantic.rs         # 语义标签推荐
│   ├── tauri.conf.json         # Tauri 配置 · 版本号 · 更新器
│   └── Cargo.toml              # Rust 依赖管理
│
├── .github/workflows/
│   └── build.yml               # CI/CD：Tag 触发 · 编译 · 签名 · 发布
│
├── package.json                # 前端依赖 · npm 脚本
└── 开发流程教程.md              # 本文档
```

**数据流向：**
```
用户操作 → React UI → invoke() → Tauri IPC → Rust 命令 → 文件系统/SQLite
                                                          ↓
                                              Event 推送 → React UI 更新
```

---

## 2. 开发环境搭建（仅首次）

### 必装工具

```powershell
# 1. Node.js 22+
winget install OpenJS.NodeJS.LTS

# 2. Rust 工具链
winget install Rustlang.Rustup
rustup default stable

# 3. Tauri CLI（自动随 npm 安装，无需单独安装）

# 4. GitHub CLI（用于发布管理）
winget install GitHub.cli
gh auth login

# 5. VS Build Tools（Tauri Windows 编译必须）
winget install Microsoft.VisualStudio.2022.BuildTools
```

### 克隆项目

```bash
git clone https://github.com/TimeTravelCoder/LedgerProMax.git
cd LedgerProMax
git checkout ProMax        # 切换到主开发分支
npm install                # 安装前端依赖
```

### 验证环境

```bash
npm run dev                # 启动开发服务器，能看到界面即成功
```

---

## 3. 立项流程

> 适用于全新项目或新功能模块从零开始的场景。

### Step 1：初始化 Tauri 项目

```bash
# 创建项目目录
mkdir MyNewApp && cd MyNewApp

# 初始化（选择 React + TypeScript 模板）
npm create tauri-app@latest ./

# 安装依赖
npm install
```

### Step 2：初始化 Git 仓库

```bash
git init
git add -A
git commit -m "init: project scaffold"

# 关联远端仓库
git remote add origin https://github.com/用户名/仓库名.git
git branch -M main
git push -u origin main
```

### Step 3：创建开发分支体系

```bash
# 主开发分支（对应本项目的 ProMax）
git checkout -b main

# 功能开发分支（按需创建）
git checkout -b feature/功能名

# 修复分支
git checkout -b fix/问题描述
```

### Step 4：生成签名密钥（一次性，务必保存）

```bash
# 生成无密码密钥对（CI 自动化必须）
$env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD = ""
npx tauri signer generate -w ~/.tauri/项目名.key --force --ci

# 查看公钥（填入 tauri.conf.json 的 pubkey 字段）
type ~/.tauri/项目名.key.pub
```

> ⚠️ **私钥文件必须妥善保管**，丢失后无法继续发布更新。

### Step 5：配置 GitHub Secrets

```bash
# 上传私钥到 GitHub（使用 GitHub CLI）
Get-Content ~/.tauri/项目名.key | gh secret set TAURI_SIGNING_PRIVATE_KEY --repo 用户名/仓库名

# 上传空密码
Write-Output "" | gh secret set TAURI_SIGNING_PRIVATE_KEY_PASSWORD --repo 用户名/仓库名

# 验证
gh secret list --repo 用户名/仓库名
```

### Step 6：配置更新器端点

编辑 `src-tauri/tauri.conf.json`：

```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://github.com/用户名/仓库名/releases/latest/download/updater.json"
      ],
      "pubkey": "这里填公钥内容"
    }
  }
}
```

---

## 4. 日常开发流程

### 启动开发环境

```bash
# 启动 Tauri 开发窗口（前端热更新 + Rust 编译）
npm run dev
```

> 第一次启动 Rust 编译较慢（约 2 分钟），后续热更新秒级响应。

### 前端开发（React / TypeScript）

**新建页面组件：**
```
src/components/pages/NewPage.tsx   ← 创建页面组件
src/App.tsx                        ← 注册到路由和标签页
```

**添加新类型：**
```typescript
// src/types/index.ts
export interface NewRecord {
  id: number;
  name: string;
}
```

**调用后端命令：**
```typescript
import { invoke } from "@tauri-apps/api/core";

// 调用 Rust 命令
const result = await invoke<ReturnType>("command_name", {
  param1: value1,
  param2: value2
});
```

### 后端开发（Rust）

**新增命令流程：**

```rust
// 1. 在对应模块文件（如 file_manager.rs）实现函数
pub fn my_new_function(param: &str) -> Result<String, String> {
    // 实现逻辑
    Ok("result".to_string())
}

// 2. 在 lib.rs 中注册为 Tauri 命令
#[tauri::command]
async fn my_new_command(param: String) -> Result<String, String> {
    file_manager::MyModule::my_new_function(&param)
}

// 3. 在 invoke_handler 中添加
.invoke_handler(tauri::generate_handler![
    // ... 已有命令
    my_new_command,   // ← 新增
])
```

### 类型检查（开发期间随时运行）

```bash
npx tsc --noEmit      # TypeScript 零错误才能提交
```

---

## 5. 版本迭代规范

### 版本号规则（语义化版本）

```
主版本.次版本.修订号
  v3  .  1  .  0

主版本（Major）：架构级重构、破坏性变更
次版本（Minor）：新增功能、重大改进
修订号（Patch）：Bug 修复、小优化
```

### 需要修改的四处版本号

每次发版都必须同步更新：

| 文件 | 修改内容 |
|------|---------|
| `package.json` | `"version": "3.1.0"` |
| `src-tauri/tauri.conf.json` | `"version": "3.1.0"` |
| `src-tauri/Cargo.toml` | `version = "3.1.0"` |
| `src/components/pages/AboutPage.tsx` | `v3.1.0`（两处硬编码） |

---

## 6. 发布上线流程

> **核心原则**：普通 push 不触发编译，只有推送版本 Tag 才启动自动构建。

### 完整发布步骤

```powershell
# ===========================
# 第①步：修改四处版本号（见上表）
# ===========================

# ===========================
# 第②步：提交代码（不触发编译）
# ===========================
git add -A
git commit -m "release: v3.1.0 - 此处填写更新内容"
git push origin ProMax

# ===========================
# 第③步：推送版本标签（触发编译！）
# ===========================
git tag v3.1.0
git push origin v3.1.0
```

### 一键发布脚本（复制修改版本号后执行）

```powershell
$ver = "3.1.0"   # ← 只需修改这里

git add -A
git commit -m "release: v$ver"
git push origin ProMax
git tag "v$ver"
git push origin "v$ver"

Write-Host "✅ 标签 v$ver 已推送，GitHub Actions 开始编译！" -ForegroundColor Green
Write-Host "📊 进度：https://github.com/TimeTravelCoder/LedgerProMax/actions" -ForegroundColor Cyan
```

### 查看编译进度

```bash
gh run list --repo TimeTravelCoder/LedgerProMax --limit 5

# 查看某次构建详细日志
gh run view <run_id> --repo TimeTravelCoder/LedgerProMax

# 只看失败步骤
gh run view <run_id> --repo TimeTravelCoder/LedgerProMax --log-failed
```

### 编译完成后 GitHub Release 包含的文件

| 文件 | 说明 |
|------|------|
| `*_x64-setup.exe` | NSIS 安装包（推荐用户下载） |
| `*_x64-setup.exe.sig` | 安装包签名（自动更新验证用） |
| `*_x64_zh-CN.msi` | WiX MSI 安装包 |
| `*_x64_zh-CN.msi.sig` | MSI 签名 |
| `updater.json` | **自动更新描述文件**（app 启动时拉取） |

---

## 7. 自动更新机制说明

```
用户启动 app
    ↓
app 向 GitHub 请求 updater.json
    ↓
比较 updater.json 中的 version 与当前版本
    ↓
发现新版本 → 弹出更新提示
    ↓
用户点击确认 → 下载 .exe/.msi
    ↓
验证 .sig 签名文件（用 tauri.conf.json 中的 pubkey 验证）
    ↓
签名合法 → 执行安装 → 重启 app ✅
```

**关键文件关系：**
- `tauri.conf.json` → `pubkey`：用于验证安装包合法性
- `~/.tauri/ledgerpromax_v3.key`：签名私钥（存本地，上传到 GitHub Secrets）
- `updater.json`：每次构建自动生成，描述最新版本下载地址和签名

---

## 8. 分支管理策略

```
ProMax（主分支）
  │
  ├── feature/功能名     功能开发完成后合并回 ProMax
  ├── fix/问题描述       Bug 修复完成后合并回 ProMax
  │
  ├── MAC                macOS 平台适配分支
  └── MAC_Fix            macOS 修复分支
```

**分支操作规范：**

```bash
# 创建功能分支
git checkout -b feature/新功能名 ProMax

# 开发完成后合并
git checkout ProMax
git merge feature/新功能名 --no-ff -m "feat: 合并新功能"
git push origin ProMax

# 删除已合并的功能分支
git branch -d feature/新功能名
git push origin --delete feature/新功能名
```

---

## 9. Commit 规范

遵循 **Conventional Commits** 规范：

```
类型(范围): 简短描述

类型说明：
  feat     - 新功能
  fix      - Bug 修复
  refactor - 代码重构（不改变功能）
  style    - 代码格式调整
  chore    - 构建/工具/依赖更新
  ci       - CI/CD 配置变更
  docs     - 文档更新
  release  - 版本发布
  bump     - 版本号变更
  perf     - 性能优化
```

**示例：**
```bash
git commit -m "feat: 增加文件预览 PDF 支持"
git commit -m "fix: 修复收集箱批量归档后列表不刷新的问题"
git commit -m "refactor: 将 App.tsx 拆分为独立页面组件"
git commit -m "release: v3.1.0 - 新增智能标签批量编辑功能"
git commit -m "ci: 修复 Windows Runner Python 编码错误"
```

---

## 10. 常见问题速查

### Q: 前端修改后看不到效果？
```bash
# Tauri 开发模式下前端热更新，无需重启
# 如果确实不更新，重启 dev 服务器
Ctrl+C  →  npm run dev
```

### Q: TypeScript 报错怎么处理？
```bash
npx tsc --noEmit   # 查看所有错误
# 修复后再次运行，确保零错误后再 commit
```

### Q: Rust 编译报错？
```bash
cd src-tauri
cargo check       # 快速检查错误（不生成可执行文件）
cargo build       # 完整编译
```

### Q: 标签打错了怎么办？
```bash
git tag -d v3.1.0                   # 删除本地标签
git push origin --delete v3.1.0    # 删除远端标签
# 修改后重新打标签
git tag v3.1.0
git push origin v3.1.0
```

### Q: GitHub Actions 失败怎么排查？
```bash
# 查看最近几次运行
gh run list --repo TimeTravelCoder/LedgerProMax --limit 5

# 查看失败步骤的日志
gh run view <run_id> --repo TimeTravelCoder/LedgerProMax --log-failed
```

### Q: 私钥丢了怎么办？
必须重新生成密钥对，并且：
1. 更新 `tauri.conf.json` 中的 `pubkey`
2. 更新 GitHub Secrets 中的 `TAURI_SIGNING_PRIVATE_KEY`
3. **注意**：新密钥签名的版本与旧密钥签名的版本不兼容，建议跳一个大版本号

---

## 附录：常用命令速查表

```bash
# 开发
npm run dev                          # 启动开发环境
npx tsc --noEmit                     # TypeScript 检查

# Git 日常
git status                           # 查看当前状态
git add -A && git commit -m "..."    # 暂存并提交
git push origin ProMax               # 推送到主分支（不触发编译）

# 发版
git tag v3.1.0 && git push origin v3.1.0   # 打标签并推送（触发编译）
gh run list --repo TimeTravelCoder/LedgerProMax   # 查看 CI 状态
gh release list --repo TimeTravelCoder/LedgerProMax  # 查看发布列表

# Secrets 管理
gh secret list --repo TimeTravelCoder/LedgerProMax
Get-Content ~/.tauri/ledgerpromax_v3.key | gh secret set TAURI_SIGNING_PRIVATE_KEY --repo TimeTravelCoder/LedgerProMax

# 本地构建（不上传，仅测试）
npm run tauri build
```

---

## 11. 换新机器继续开发（Windows / Mac 通用）

> 换电脑、或在 Mac 上继续开发时，按此章节操作。

### 密钥文件位置（已备份到 OneDrive）

```
C:\Users\Apollo\OneDrive\C.档案\密钥\
├── ledgerpromax_v3.key       ← 私钥（绝密，勿外泄）
└── ledgerpromax_v3.key.pub   ← 公钥（已内嵌于项目）
```

> **OneDrive 自动同步**，新机器登录 OneDrive 账号后即可访问。

---

### Step 1：安装开发环境

**Windows（PowerShell）：**
```powershell
winget install OpenJS.NodeJS.LTS
winget install Rustlang.Rustup
winget install GitHub.cli
rustup default stable
# Tauri Windows 编译需要 VS Build Tools
winget install Microsoft.VisualStudio.2022.BuildTools
```

**Mac（Terminal）：**
```bash
# 安装 Homebrew（如未安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装工具链
brew install node@22
brew install rustup-init && rustup-init
brew install gh

# Tauri Mac 编译依赖
xcode-select --install
```

---

### Step 2：克隆项目

```bash
git clone https://github.com/TimeTravelCoder/LedgerProMax.git
cd LedgerProMax
git checkout ProMax
npm install
```

---

### Step 3：恢复签名私钥

**Windows：**
```powershell
# 从 OneDrive 复制到 Tauri 标准目录
New-Item -ItemType Directory -Force "$env:USERPROFILE\.tauri"
Copy-Item "C:\Users\Apollo\OneDrive\C.档案\密钥\ledgerpromax_v3.key" "$env:USERPROFILE\.tauri\ledgerpromax_v3.key"
```

**Mac：**
```bash
# OneDrive 同步目录（根据实际路径调整）
mkdir -p ~/.tauri
cp ~/Library/CloudStorage/OneDrive-Personal/C.档案/密钥/ledgerpromax_v3.key ~/.tauri/ledgerpromax_v3.key
chmod 600 ~/.tauri/ledgerpromax_v3.key   # 设置私钥权限
```

---

### Step 4：配置 GitHub CLI 登录

```bash
gh auth login
# 选择：GitHub.com → HTTPS → 浏览器登录
```

验证登录成功：
```bash
gh auth status
```

---

### Step 5：验证环境可用

```bash
# 启动开发模式，看到界面即成功
npm run dev
```

---

### Step 6：发版时重新配置 Secrets（仅首次换机）

> 如果是首次在新机器上操作发版，需要重新上传私钥到 GitHub Secrets：

```bash
# Windows
Get-Content "$env:USERPROFILE\.tauri\ledgerpromax_v3.key" | gh secret set TAURI_SIGNING_PRIVATE_KEY --repo TimeTravelCoder/LedgerProMax

# Mac / Linux
cat ~/.tauri/ledgerpromax_v3.key | gh secret set TAURI_SIGNING_PRIVATE_KEY --repo TimeTravelCoder/LedgerProMax

# 空密码
echo "" | gh secret set TAURI_SIGNING_PRIVATE_KEY_PASSWORD --repo TimeTravelCoder/LedgerProMax

# 验证
gh secret list --repo TimeTravelCoder/LedgerProMax
```

> ✅ GitHub Secrets 是云端存储的，通常换机不需要重新设置。只有在 Secrets 被误删时才需要此操作。

---

### 换机完整检查清单

```
[ ] Node.js 22+ 已安装
[ ] Rust stable 已安装
[ ] GitHub CLI 已安装并登录
[ ] 项目已克隆，npm install 完成
[ ] 私钥文件已恢复到 ~/.tauri/ledgerpromax_v3.key
[ ] npm run dev 可以正常启动
[ ] gh auth status 显示已登录 TimeTravelCoder
```

---

*最后更新：2026-06-07 · 版本 v3.0.0*
